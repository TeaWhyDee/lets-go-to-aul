a = "abcd"

function f(b: num) -> num
    printf("b = %.3f\n", b)
    return b
end

function g(b: str) -> str
    printf("b = %s\n", b)
    return b
end

c = g(a)
printf("outside: %s\n", c)

-- a = 2
